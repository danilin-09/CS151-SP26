#include <iostream>
#include <string>
#include <cmath>

using namespace std;

int main(){
    double y = cos(60);
    cout << y << endl;

    double a = sin(90);
    cout << a << endl;

    double b = tan(45);
    cout << b << endl;

    double c = sqrt(25);
    cout << c << endl;

    double d = abs(-49);
    cout << d << endl;

    //powers (exponents)
    cout << "2 to the 3rd power is " << pow(2, 3) << endl;

    return 0;
}